// Single source of truth for store-policy text that needs to show up in
// more than one place (checkout, order tracking, the invoice PDF) so it
// never drifts out of sync between them.
export const NO_RETURNS_NOTE =
  "All sales are final. We do not currently accept returns or exchanges.";
